<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="TioUi.Demo.Pages.TimeOnlyPickerPage"
    x:DataType="pages:TimeOnlyPickerDemoViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:TioUi.Demo.Pages"
    xmlns:tio="https://github.com/tiouoo/TioUi.Avalonia"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <tio:GroupBorder
        Header="TimeOnlyPicker"
        Margin="15"
        Padding="15">
        <ScrollViewer>
            <StackPanel HorizontalAlignment="Left" Spacing="20">
                <ToggleSwitch Content="Need Confirm" Name="needConfirm" />

                <TextBlock Text="{Binding #picker.SelectedTime}" />

                <TextBox
                    InnerLeftContent="Display Format"
                    Name="displayFormat"
                    Text="HH:mm:ss"
                    Width="300" />
                <TextBox
                    InnerLeftContent="Panel Format"
                    Name="panelFormat"
                    Text="tt HH mm ss"
                    Width="300" />

                <TextBlock Text="Default" />
                <tio:TimeOnlyPicker
                    DisplayFormat="{Binding #displayFormat.Text}"
                    Name="picker"
                    NeedConfirmation="{Binding #needConfirm.IsChecked}"
                    PanelFormat="{Binding #panelFormat.Text}"
                    Width="200" />

                <TextBlock Text="Clear Button" />
                <tio:TimeOnlyPicker
                    Classes="ClearButton"
                    DisplayFormat="{Binding #displayFormat.Text}"
                    NeedConfirmation="True"
                    PanelFormat="{Binding #panelFormat.Text}"
                    Width="300" />

                <TextBlock Text="Binding" />
                <tio:TimeOnlyPicker
                    Classes="ClearButton"
                    DisplayFormat="{Binding #displayFormat.Text}"
                    PanelFormat="{Binding #panelFormat.Text}"
                    SelectedTime="{Binding Time}"
                    Width="300" />
            </StackPanel>
        </ScrollViewer>
    </tio:GroupBorder>
</UserControl>
