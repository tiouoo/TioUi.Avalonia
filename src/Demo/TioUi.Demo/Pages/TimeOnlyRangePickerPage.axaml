<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="TioUi.Demo.Pages.TimeOnlyRangePickerPage"
    x:DataType="pages:TimeOnlyRangePickerDemoViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:TioUi.Demo.Pages"
    xmlns:tio="https://github.com/tiouoo/TioUi.Avalonia"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <tio:GroupBorder
        Header="TimeOnlyRangePicker"
        Margin="15"
        Padding="15">
        <ScrollViewer>
            <StackPanel HorizontalAlignment="Left" Spacing="20">
                <TextBox
                    InnerLeftContent="Display Format"
                    Name="displayFormat"
                    Text="HH:mm:ss"
                    Width="300" />
                <TextBox
                    InnerLeftContent="Panel Format"
                    Name="panelFormat"
                    Text="tt HH mm ss"
                    Width="300" />

                <TextBlock Text="Default" />
                <tio:TimeOnlyRangePicker
                    DisplayFormat="{Binding #displayFormat.Text}"
                    PanelFormat="{Binding #panelFormat.Text}"
                    Width="300" />

                <TextBlock Text="Initialized by Binding" />
                <tio:TimeOnlyRangePicker
                    DisplayFormat="{Binding #displayFormat.Text}"
                    PanelFormat="{Binding #panelFormat.Text}"
                    SelectedEndTime="{Binding EndTime}"
                    SelectedStartTime="{Binding StartTime}"
                    Width="300" />

                <TextBlock Text="Clear Button" />
                <tio:TimeOnlyRangePicker
                    Classes="ClearButton"
                    DisplayFormat="{Binding #displayFormat.Text}"
                    PanelFormat="{Binding #panelFormat.Text}"
                    Width="300" />

                <ToggleSwitch Content="Need Confirm" Name="needConfirm" />
                <tio:TimeOnlyRangePicker
                    DisplayFormat="{Binding #displayFormat.Text}"
                    Name="confirmPicker"
                    NeedConfirmation="{Binding #needConfirm.IsChecked}"
                    PanelFormat="{Binding #panelFormat.Text}"
                    Width="300" />
            </StackPanel>
        </ScrollViewer>
    </tio:GroupBorder>
</UserControl>
